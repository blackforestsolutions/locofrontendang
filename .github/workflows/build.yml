name: Angular CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Install Dependencies
        run: yarn install

      - name: Lint
        run: yarn run lint

      - name: Unit Tests
        run: yarn run test:pipeline

      - name: Build
        run: yarn run build
